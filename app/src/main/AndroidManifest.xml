<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AccueilMbileProjtct">

        <!-- Activité principale (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activités existantes -->
        <activity
            android:name=".CategoryActivity"
            android:exported="false" />
        <activity
            android:name=".CartActivity"
            android:exported="false" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".WishlistActivity"
            android:exported="false"/>
        <activity android:name=".ParfumActivity" />
        <activity android:name=".MakeupActivity" />
        <activity android:name=".SkincareActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ProductDetailActivity" />
        <activity
            android:name=".ChatbotActivity"
            android:exported="true" />
        <activity android:name=".NotificationsActivity" />

        <!-- Activités de paiement (nouvelles) -->
        <activity
            android:name=".CheckoutActivity"
            android:exported="false"
            android:label="Finaliser la commande" />

        <activity
            android:name=".PaymentMethodActivity"
            android:exported="false"
            android:label="Mode de paiement"
            android:screenOrientation="portrait" />

        <activity
            android:name=".CardPaymentActivity"
            android:exported="false"
            android:label="Paiement sécurisé"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ConfirmationActivity"
            android:exported="false"
            android:label="Confirmation de commande"
            android:screenOrientation="portrait"
            android:noHistory="true"
            android:launchMode="singleTop" />

    </application>
</manifest>